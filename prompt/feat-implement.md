# 기능 수정/추가 작업 프롬프트 (이전됨)

> **본 문서는 슬래시 커맨드로 이전되었다.** 본 파일은 리다이렉트 스텁이다 — 내용을 여기에 다시 추가하지 않는다.

- **원본 파이프라인**: `.claude/commands/feat-implement.md` → `/feat-implement <기능명/요구사항 요약>` 으로 호출
- Phase 0~7 (도메인+코드 resolve → 규모 판단 → 문서 → 설계 → 계획 → 계획 확인 → 구현 → 검증 → 테스트 → 완료) + HITL 게이트(법무/결제/권한/DB파괴적) + 워크트리 격리(동시 실행 경합 차단)
